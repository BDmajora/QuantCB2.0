# QuantCB2.0

# Fast syntax and type checking (No binary generation)
cargo check

# Compile and execute (Debug mode - slow execution)
cargo run

# Compile and execute with optimizations (Release mode - for training/inference)
cargo run --release